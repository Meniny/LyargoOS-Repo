# Package installation module
# Uses selections from pkgchooser module

---
backend: xbps

operations:
  - install:
    - calamares
    - lyargoos-calamares-config
    # pkgchooser selections are appended at runtime
